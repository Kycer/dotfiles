(include "bspwm/bspwm.yuck")

; (defwidget bar []
;   (centerbox :orientation "h"
;     (workspaces)
;     (json)
;     (jsona)))



(defwindow bar
  :monitor 0
  :windowtype "dock"
  :stacking "bg"
  :geometry (geometry :x "0%"
                      :y "1%"
                      :width "90%"
                      :height "10px"
                      :anchor "top center")
  :reserve (struts :side "top" :distance "0%")
  (json))
